<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Company Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/styles.css">
<style>
    h2{
        text-align: center;
        margin: 30px;
        color: #ff4081;
    }
    .btn-primary{
        margin-left: 100px;
    }
    
</style>
   
</head>
<body>
    <header class="py-3">
        <div class="container d-flex justify-content-between align-items-center">
            <div class="logo">
                <h1 class="h3">Company Dashboard</h1>
                <p class="m-0">Welcome, <%= user.companyname %></p>
            </div>
            <nav>
                <a href="/profile">Profile</a> | <a href="/logout">Logout</a> | <a href="internship">Post</a> |  <a href="/posting">Home</a> 
            </nav>
        </div>
    </header>
    <div class="container">
        <h2>Your Internships</h2>
        <div class="row">
            <% internships.forEach(internship => { %>
                <div class="col-lg-4 col-md-6">
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title"><%= internship.job_title %></h5>
                            <p class="card-text"><%= internship.description %></p>
                            <a href="/internships/<%= internship._id %>" class="btn btn-primary">View Details</a> <br><br>
                            <form action="/edit-internship/<%= internship._id %>" method="get">
                                <button type="submit" class="btn btn-secondary">Edit</button>  
                            </form> <br>  
                            <form action="/delete-internship/<%= internship._id %>" method="post">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
